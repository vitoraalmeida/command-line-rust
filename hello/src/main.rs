fn main() {
    println!("Hello, world!");
    // programas em rust retornam o statux exit 0 por padrão
    // é importante retornar status pois assim o comando pode ser
    // usado de forma composta com outros
    // ex.:
    // $ true && ls
    // só vai executar ls se true retornar um exit status 0 
}
